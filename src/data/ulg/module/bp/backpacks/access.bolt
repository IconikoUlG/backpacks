from ./backpacks import getAll, getMainVariants, getRecipes, getConversions

def getBackpack(id : str):
    for bp in getAll():
       if bp.Id() == id:
            return bp

fix25_conversions = getConversions()