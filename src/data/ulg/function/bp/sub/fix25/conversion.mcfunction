from @configs import fix25_conversions

for e in fix25_conversions:
    if data storage ulg:macro tick{"oldCMD":(e.old_cmd)} run data modify storage ulg:macro packet set value {"model":(e.model),"colors":str(e.colors)}